const nums = [1, 2, 3, 4, 5]

// For com prpósito

let resultado = nums.map(function(e){
    return e * 2
})
console.log(resultado)
//o Map gera um novo array, e nesse caso com todos os valores multiplicados por dois